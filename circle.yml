machine:
  ruby:
    version: 2.3.0

  services:
    - rabbitmq-server

database:
  override: [] # Skip default command: `bundle exec rake db:create db:schema:load --trace`

test:
  pre:
    - gem update bundler
