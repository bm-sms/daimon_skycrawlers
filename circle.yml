machine:
  ruby:
    version: 2.3.0

  services:
    - rabbitmq-server

database:
  override:
    - echo 'skip' # Skip default command: `bundle exec rake db:create db:schema:load --trace`

test:
  pre:
    - gem update bundler

  override:
    - echo 'noop' # XXX This is debug
